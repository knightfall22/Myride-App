import React from "react";
import { Text, StyleSheet, View } from "react-native";

const DriverHomeScreen = ({ navigation }) => {
  return <Text>Driver Home Screen</Text>;
};

const styles = StyleSheet.create({});

export default DriverHomeScreen;
